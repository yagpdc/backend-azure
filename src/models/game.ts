// TODO: Define Game entity schema/DTOs using zod when requirements are clear.
// import { z } from "zod";
// export const CreateGameDtoSchema = z.object({
//   // TODO: add game fields (e.g., title, genre, releaseDate)
// });
// export type CreateGameDto = z.infer<typeof CreateGameDtoSchema>;
